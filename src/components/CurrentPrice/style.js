import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    headerPrice: {
        width: "100%",
        height: "auto",
        alignItems: "center",
    },
    currentPrice: {
        color: 'white',
        fontSize: 30,
        fontWeight: 'bold',
    },
    textPrice: {
        color: "white",
        fontSize: 16,
        fontWeight: "bold",
        marginBottom: 20,
    },
})

export default styles;